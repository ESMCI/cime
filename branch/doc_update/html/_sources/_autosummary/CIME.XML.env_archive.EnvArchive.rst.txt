..
  class.rst

CIME.XML.env\_archive.EnvArchive
================================

.. currentmodule:: CIME.XML.env_archive

.. autoclass:: EnvArchive
    :members:
    :show-inheritance:
    :inherited-members:

    
        
            .. rubric:: Methods

            .. autosummary::
                :nosignatures:
                
                    ~EnvArchive.__init__
                    ~EnvArchive.add_child
                    ~EnvArchive.add_elements_by_group
                    ~EnvArchive.attrib
                    ~EnvArchive.change_file
                    ~EnvArchive.check_if_comp_var
                    ~EnvArchive.check_timestamp
                    ~EnvArchive.cleanupnode
                    ~EnvArchive.compare_xml
                    ~EnvArchive.copy
                    ~EnvArchive.exclude_testing
                    ~EnvArchive.get
                    ~EnvArchive.get_all_hist_files
                    ~EnvArchive.get_child
                    ~EnvArchive.get_child_content
                    ~EnvArchive.get_children
                    ~EnvArchive.get_default_value
                    ~EnvArchive.get_description
                    ~EnvArchive.get_element_text
                    ~EnvArchive.get_elements_from_child_content
                    ~EnvArchive.get_entries
                    ~EnvArchive.get_entry
                    ~EnvArchive.get_entry_attributes
                    ~EnvArchive.get_entry_info
                    ~EnvArchive.get_entry_value
                    ~EnvArchive.get_groups
                    ~EnvArchive.get_hist_file_ext_regexes
                    ~EnvArchive.get_hist_file_extensions
                    ~EnvArchive.get_id
                    ~EnvArchive.get_latest_hist_files
                    ~EnvArchive.get_node_element_info
                    ~EnvArchive.get_nodes_by_id
                    ~EnvArchive.get_optional_child
                    ~EnvArchive.get_raw_record
                    ~EnvArchive.get_resolved_value
                    ~EnvArchive.get_rest_file_extensions
                    ~EnvArchive.get_rpointer_contents
                    ~EnvArchive.get_type_info
                    ~EnvArchive.get_valid_value_string
                    ~EnvArchive.get_valid_values
                    ~EnvArchive.get_value
                    ~EnvArchive.get_value_match
                    ~EnvArchive.get_values
                    ~EnvArchive.get_version
                    ~EnvArchive.has
                    ~EnvArchive.invalidate
                    ~EnvArchive.lock
                    ~EnvArchive.make_child
                    ~EnvArchive.make_child_comment
                    ~EnvArchive.name
                    ~EnvArchive.overwrite_existing_entries
                    ~EnvArchive.pop
                    ~EnvArchive.read
                    ~EnvArchive.read_fd
                    ~EnvArchive.remove_child
                    ~EnvArchive.scan_child
                    ~EnvArchive.scan_children
                    ~EnvArchive.scan_optional_child
                    ~EnvArchive.set
                    ~EnvArchive.set_components
                    ~EnvArchive.set_default_value
                    ~EnvArchive.set_element_text
                    ~EnvArchive.set_name
                    ~EnvArchive.set_text
                    ~EnvArchive.set_valid_values
                    ~EnvArchive.set_value
                    ~EnvArchive.text
                    ~EnvArchive.to_string
                    ~EnvArchive.unlock
                    ~EnvArchive.validate_timestamp
                    ~EnvArchive.validate_xml_file
                    ~EnvArchive.write
        
    