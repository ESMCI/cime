..
  class.rst

CIME.XML.env\_mach\_specific.EnvMachSpecific
============================================

.. currentmodule:: CIME.XML.env_mach_specific

.. autoclass:: EnvMachSpecific
    :members:
    :show-inheritance:
    :inherited-members:

    
        
            .. rubric:: Methods

            .. autosummary::
                :nosignatures:
                
                    ~EnvMachSpecific.__init__
                    ~EnvMachSpecific.add_child
                    ~EnvMachSpecific.add_elements_by_group
                    ~EnvMachSpecific.allow_error
                    ~EnvMachSpecific.attrib
                    ~EnvMachSpecific.change_file
                    ~EnvMachSpecific.check_if_comp_var
                    ~EnvMachSpecific.check_timestamp
                    ~EnvMachSpecific.cleanupnode
                    ~EnvMachSpecific.compare_xml
                    ~EnvMachSpecific.copy
                    ~EnvMachSpecific.get
                    ~EnvMachSpecific.get_aprun_args
                    ~EnvMachSpecific.get_aprun_mode
                    ~EnvMachSpecific.get_child
                    ~EnvMachSpecific.get_child_content
                    ~EnvMachSpecific.get_children
                    ~EnvMachSpecific.get_default_value
                    ~EnvMachSpecific.get_description
                    ~EnvMachSpecific.get_element_text
                    ~EnvMachSpecific.get_elements_from_child_content
                    ~EnvMachSpecific.get_groups
                    ~EnvMachSpecific.get_id
                    ~EnvMachSpecific.get_module_system_cmd_path
                    ~EnvMachSpecific.get_module_system_init_path
                    ~EnvMachSpecific.get_module_system_type
                    ~EnvMachSpecific.get_mpirun
                    ~EnvMachSpecific.get_node_element_info
                    ~EnvMachSpecific.get_nodes_by_id
                    ~EnvMachSpecific.get_optional_child
                    ~EnvMachSpecific.get_overrides_nodes
                    ~EnvMachSpecific.get_raw_record
                    ~EnvMachSpecific.get_resolved_value
                    ~EnvMachSpecific.get_type_info
                    ~EnvMachSpecific.get_valid_value_string
                    ~EnvMachSpecific.get_valid_values
                    ~EnvMachSpecific.get_value
                    ~EnvMachSpecific.get_value_match
                    ~EnvMachSpecific.get_values
                    ~EnvMachSpecific.get_version
                    ~EnvMachSpecific.has
                    ~EnvMachSpecific.invalidate
                    ~EnvMachSpecific.list_modules
                    ~EnvMachSpecific.load_env
                    ~EnvMachSpecific.lock
                    ~EnvMachSpecific.make_child
                    ~EnvMachSpecific.make_child_comment
                    ~EnvMachSpecific.make_env_mach_specific_file
                    ~EnvMachSpecific.name
                    ~EnvMachSpecific.overwrite_existing_entries
                    ~EnvMachSpecific.pop
                    ~EnvMachSpecific.populate
                    ~EnvMachSpecific.read
                    ~EnvMachSpecific.read_fd
                    ~EnvMachSpecific.remove_child
                    ~EnvMachSpecific.save_all_env_info
                    ~EnvMachSpecific.scan_child
                    ~EnvMachSpecific.scan_children
                    ~EnvMachSpecific.scan_optional_child
                    ~EnvMachSpecific.set
                    ~EnvMachSpecific.set_components
                    ~EnvMachSpecific.set_default_value
                    ~EnvMachSpecific.set_element_text
                    ~EnvMachSpecific.set_name
                    ~EnvMachSpecific.set_text
                    ~EnvMachSpecific.set_valid_values
                    ~EnvMachSpecific.set_value
                    ~EnvMachSpecific.text
                    ~EnvMachSpecific.to_string
                    ~EnvMachSpecific.unlock
                    ~EnvMachSpecific.validate_timestamp
                    ~EnvMachSpecific.validate_xml_file
                    ~EnvMachSpecific.write
        
    