..
  class.rst

CIME.XML.testlist.Testlist
==========================

.. currentmodule:: CIME.XML.testlist

.. autoclass:: Testlist
    :members:
    :show-inheritance:
    :inherited-members:

    
        
            .. rubric:: Methods

            .. autosummary::
                :nosignatures:
                
                    ~Testlist.__init__
                    ~Testlist.add_child
                    ~Testlist.attrib
                    ~Testlist.change_file
                    ~Testlist.check_timestamp
                    ~Testlist.copy
                    ~Testlist.get
                    ~Testlist.get_child
                    ~Testlist.get_children
                    ~Testlist.get_element_text
                    ~Testlist.get_id
                    ~Testlist.get_optional_child
                    ~Testlist.get_raw_record
                    ~Testlist.get_resolved_value
                    ~Testlist.get_tests
                    ~Testlist.get_value
                    ~Testlist.get_values
                    ~Testlist.get_version
                    ~Testlist.has
                    ~Testlist.invalidate
                    ~Testlist.lock
                    ~Testlist.make_child
                    ~Testlist.make_child_comment
                    ~Testlist.name
                    ~Testlist.pop
                    ~Testlist.read
                    ~Testlist.read_fd
                    ~Testlist.remove_child
                    ~Testlist.scan_child
                    ~Testlist.scan_children
                    ~Testlist.scan_optional_child
                    ~Testlist.set
                    ~Testlist.set_element_text
                    ~Testlist.set_name
                    ~Testlist.set_text
                    ~Testlist.set_value
                    ~Testlist.text
                    ~Testlist.to_string
                    ~Testlist.unlock
                    ~Testlist.validate_timestamp
                    ~Testlist.validate_xml_file
                    ~Testlist.write
        
    